# Copy this file to .env and fill in the values.
# .env is gitignored, so it stays local.

# eBird API key — request a free one at https://ebird.org/api/keygen
EBIRD_API_KEY=

# Optional: API key for LLM enrichment (enrichment runs when llm endpoint/models and this key are set)
# BOTD_LLM_API_KEY=
